[package]
name = "simple-debug"
version = "0.1.0"
edition = "2024"
authors = ["ziprangga"]
license = "MIT or Apache-2.0"

[features]
default = ["console"]
console = []
buffer = []



[dependencies]
chrono = "0.4.44"
tokio = { version = "1.50", features = ["full"] }

[workspace]
members = [
    ".",
    "sample"
]
